# Example environment file for the API server
# For development, we're using SQLite (no setup needed)
# After running push, the database file will be created at the root.

DATABASE_URL=./db.sqlite
JWT_SECRET=smartwarehouse-secret-key
PORT=8080
